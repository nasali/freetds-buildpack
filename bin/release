#!/usr/bin/env ruby

VENDOR_DIR="#{ARGV[0]}/vendor" #The FREETDS_DIR needs to be absolute is why

puts <<-CONF
config_vars:
  PATH: "#{ENV['PATH']}:vendor/freetds/bin"
  LD_LIBRARY_PATH: "#{ENV['LD_LIBRARY_PATH']}:vendor/freetds/lib"
  FREETDS_DIR: #{VENDOR_DIR}/freetds
CONF
